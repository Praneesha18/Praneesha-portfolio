# Praneesha Portfolio

React + Vite personal portfolio for Bheemarasetty Praneesha.

## Run locally

```bash
npm install
npm run dev
```

Use `npm run build` to create the production bundle, then `npm run preview` to check it locally. The downloadable résumé is served from `public/resume/PraneeshaResumeUpdated.pdf`.
