import { ArrowUpRight } from 'lucide-react'
export default function Button({ children, href, secondary=false, className='', ...props }) { const style = `inline-flex items-center justify-center gap-2 rounded-full px-5 py-3 text-sm font-bold transition duration-200 focus:outline-none focus:ring-2 focus:ring-saffron focus:ring-offset-2 ${secondary ? 'border border-ink/20 bg-transparent hover:border-ink hover:bg-ink hover:text-paper' : 'bg-saffron text-white hover:-translate-y-0.5 hover:bg-ink'} ${className}`; return href ? <a className={style} href={href} {...props}>{children}<ArrowUpRight size={16}/></a> : <button className={style} {...props}>{children}</button> }
