import { ArrowUpRight } from 'lucide-react'
export default function ExperienceCard({ item, index }) { return <article className="grid gap-4 border-t border-ink/15 py-7 md:grid-cols-[4rem_1fr_auto]"><span className="font-display text-sm font-bold text-saffron">0{index + 1}</span><div><h3 className="text-lg font-bold">{item.role} <span className="font-normal text-ink/55">/ {item.company}</span></h3>{item.place && <p className="mt-1 text-sm text-ink/50">{item.place}</p>}<ul className="mt-4 space-y-2 text-sm leading-6 text-ink/70">{item.points.map(point=><li key={point} className="before:mr-2 before:text-saffron before:content-['—']">{point}</li>)}</ul>{item.link && <a href={item.link.url} target="_blank" rel="noreferrer" className="mt-4 inline-flex items-center gap-1 text-sm font-bold text-moss hover:underline">{item.link.label}<ArrowUpRight size={15}/></a>}</div><time className="text-sm text-ink/50 md:text-right">{item.date}</time></article> }
