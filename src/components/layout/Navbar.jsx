import { Menu, X, Moon, Sun } from 'lucide-react'
import { Link, useLocation } from 'react-router-dom'
import { useEffect, useState } from 'react'
import { links } from '../../data/portfolioData'
const nav = [['About','#about'],['Experience','#experience'],['Projects','#projects'],['Education','#education'],['Contact','#contact']]
export default function Navbar(){ const [open,setOpen]=useState(false); const [theme,setTheme]=useState(()=>localStorage.getItem('theme') || 'dark'); const {pathname}=useLocation(); const close=()=>setOpen(false); const navHref = href => pathname === '/' ? href : `/${href}`; const toggleTheme=()=>setTheme(value=>value === 'dark' ? 'light' : 'dark'); useEffect(() => { document.documentElement.dataset.theme = theme; localStorage.setItem('theme', theme) }, [theme]); return <header className="sticky top-0 z-50 border-b border-ink/10 bg-paper/90 backdrop-blur"><nav className="container flex h-[72px] items-center justify-between" aria-label="Main navigation"><Link to="/" className="font-display text-lg font-bold tracking-tight">praneesha<span className="text-saffron">.</span></Link><div className="hidden items-center gap-6 lg:flex">{nav.map(([name,href])=><a key={name} className="nav-link" href={navHref(href)}>{name}</a>)}<button onClick={toggleTheme} className="theme-toggle" aria-label={`Switch to ${theme === 'dark' ? 'light' : 'dark'} theme`}>{theme === 'dark' ? <Sun size={16}/> : <Moon size={16}/>}<span>{theme} mode</span></button><a href={links.resume} download className="resume-link">Resume ↓</a></div><div className="flex items-center gap-2 lg:hidden"><button onClick={toggleTheme} className="theme-toggle theme-toggle--mobile" aria-label={`Switch to ${theme === 'dark' ? 'light' : 'dark'} theme`}>{theme === 'dark' ? <Sun size={16}/> : <Moon size={16}/>}<span>{theme}</span></button><button className="rounded p-2" aria-expanded={open} aria-label="Toggle menu" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div></nav>{open&&<div className="border-t border-ink/10 bg-paper px-5 pb-5 lg:hidden">{nav.map(([name,href])=><a key={name} className="block py-3 font-bold" onClick={close} href={navHref(href)}>{name}</a>)}<a href={links.resume} download className="mt-2 inline-block font-bold text-saffron">Download Resume ↓</a></div>}</header> }
